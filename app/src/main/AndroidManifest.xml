<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.crateus.kariri.petnatunutri">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetNatuNutri">
        <activity android:name=".activity.MeusDadosActivity"></activity>
        <activity android:name=".activity.AtribuirDietaActivity" />
        <activity android:name=".activity.CadastrarNovaDietaActivity" />
        <activity android:name=".activity.CadastrarUsuarioActivity" />
        <activity android:name=".activity.PainelAdmActivity" />
        <activity android:name=".activity.PaginaInicialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>